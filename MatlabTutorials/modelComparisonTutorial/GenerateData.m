function [x,yTrue,yObs,yMean] = GenerateData(theta,nX,nReplications,noiseSd)% [x,yTrue,yObs,yMean] = GenerateData(theta,nX,nReplications,noiseSd)%% Generate simulated data according to passed polynomial and noise.%% 7/27/04	dhb		Pulled it out.% Define range of xxLow = 0;xHigh = 1;% Generate noise free data y and make a little plotx = linspace(xLow,xHigh,nX)';X = [];for i = 1:length(theta)	X = [X x.^(i-1)];endyTrue = X*theta;% Now we generate experimental data.  This% is the true data plus normally distributed noise.% The observations end up in the columsn of yObs;noiseDraws = noiseSd*randn(nX,nReplications);yObs = yTrue(:,ones(1,nReplications))+noiseDraws;yMean = mean(yObs,2);